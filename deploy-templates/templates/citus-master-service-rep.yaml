apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.citus.mastersConfig.replicaName }}
  labels:
    app: {{ .Values.citus.mastersConfig.replicaName }}
spec:
  selector:
    app: {{ .Values.citus.mastersConfig.replicaName }}
  clusterIP: None
  ports:
  - port: {{ .Values.citus.mastersConfig.containers.port }}
