kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Values.citus.mastersConfig.volumes.claimName }}
spec:
  storageClassName: {{ .Values.storageClass }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.citus.mastersConfig.volumes.size }}
