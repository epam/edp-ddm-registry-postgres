apiVersion: v1
kind: Service
metadata:
  annotations:
    service.alpha.kubernetes.io/tolerate-unready-endpoints: "true"
  name: {{ .Values.citus.workersConfig.replicaName }}
  labels:
    app: {{ .Values.citus.workersConfig.replicaName }}
spec:
  selector:
    app: {{ .Values.citus.workersConfig.replicaName }}
  clusterIP: None
  ports:
  - port: {{ .Values.citus.workersConfig.containers.port }}
  publishNotReadyAddresses: true
